const router = require("express").Router();
const WishList = require("../models/wishlistModel");

// add the wishlist to the particular person's wishlist
// app.post('/:userId', () => {
    
// })

module.exports = router;
